const images = {
    logo: require('./logo.png').default?.src,
    logoLight: require('./logo-light.png').default?.src,
    flag: require('./flag.png').default?.src,
    app: require('./appshot.png').default?.src,
    user: require('./user.png').default?.src,
    issuePic: require('./issuepic.png').default?.src,
}

export default images