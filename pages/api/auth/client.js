// Register users who will seek assistance from organizations

export default function handler(req, res) {
    res.status(200).json({ note: 'Register users who will seek assistance from organizations' })
  }